import { StyleSheet } from "react-native";

const x = StyleSheet.create({
textBox: {
    marginTop: 20,
    fontFamily: 'sans',
    color: 'green',
    padding: 10,
    letterSpacing: 5
}
})

export default x;